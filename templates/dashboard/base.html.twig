<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}ÉcoCollecte{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🌱</text></svg>">
        
        <!-- Bootstrap CSS -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
        
        <!-- Font Awesome -->
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
        
        {% block stylesheets %}
        <style>
            :root {
                --primary-green: #88C9A1;
                --secondary-green: #B8DFC7;
                --accent-green: #5AA678;
                --light-green: #E8F4EC;
                --dark-green: #2D5A3D;
                --text-dark: #2C3E2D;
                --text-light: #5D735F;
            }
            
            body {
                background-color: #f8fdf9;
                color: var(--text-dark);
                font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
                line-height: 1.6;
            }
            
            .navbar {
                background: linear-gradient(135deg, var(--dark-green) 0%, var(--accent-green) 100%) !important;
                box-shadow: 0 2px 10px rgba(45, 90, 61, 0.2);
            }
            
            .navbar-brand {
                font-weight: 700;
                font-size: 1.5rem;
            }
            
            .eco-card {
                border: none;
                border-radius: 15px;
                box-shadow: 0 4px 15px rgba(136, 201, 161, 0.2);
                transition: transform 0.3s ease, box-shadow 0.3s ease;
                background: linear-gradient(135deg, var(--light-green) 0%, #ffffff 100%);
            }
            
            .eco-card:hover {
                transform: translateY(-5px);
                box-shadow: 0 8px 25px rgba(136, 201, 161, 0.3);
            }
            
            .stats-card {
                background: linear-gradient(135deg, var(--primary-green) 0%, var(--accent-green) 100%);
                color: white;
                border-radius: 15px;
                padding: 1.5rem;
                text-align: center;
            }
            
            .stats-card .card-title {
                font-size: 1rem;
                font-weight: 300;
                margin-bottom: 0.5rem;
                opacity: 0.9;
            }
            
            .stats-card .display-4 {
                font-weight: 700;
                font-size: 2.5rem;
                margin-bottom: 0;
            }
            
            .eco-btn {
                background: linear-gradient(135deg, var(--primary-green) 0%, var(--accent-green) 100%);
                border: none;
                border-radius: 25px;
                padding: 10px 25px;
                color: white;
                font-weight: 600;
                transition: all 0.3s ease;
                text-decoration: none;
                display: inline-block;
            }
            
            .eco-btn:hover {
                transform: translateY(-2px);
                box-shadow: 0 5px 15px rgba(90, 166, 120, 0.4);
                color: white;
            }
            
            .eco-btn-secondary {
                background: linear-gradient(135deg, var(--secondary-green) 0%, var(--primary-green) 100%);
            }
            
            .eco-table {
                background: white;
                border-radius: 10px;
                overflow: hidden;
                box-shadow: 0 4px 15px rgba(136, 201, 161, 0.1);
            }
            
            .eco-table thead {
                background: linear-gradient(135deg, var(--primary-green) 0%, var(--accent-green) 100%);
                color: white;
            }
            
            .eco-table th {
                border: none;
                font-weight: 600;
                padding: 1rem;
            }
            
            .eco-table td {
                padding: 1rem;
                border-bottom: 1px solid var(--light-green);
            }
            
            .eco-table tbody tr:hover {
                background-color: var(--light-green);
            }
            
            .page-title {
                color: var(--dark-green);
                font-weight: 700;
                margin-bottom: 2rem;
            }
            
            .section-title {
                color: var(--dark-green);
                font-weight: 600;
                margin-bottom: 1.5rem;
                border-left: 4px solid var(--primary-green);
                padding-left: 1rem;
            }
            
            .quick-nav {
                background: linear-gradient(135deg, var(--light-green) 0%, #ffffff 100%);
                border-radius: 15px;
                padding: 2rem;
                text-align: center;
            }
            
            .badge-eco {
                background-color: var(--primary-green);
                color: white;
                border-radius: 20px;
                padding: 0.3rem 0.8rem;
                font-weight: 500;
                font-size: 0.8rem;
            }
            
            .page-header {
                background: linear-gradient(135deg, var(--light-green) 0%, #ffffff 100%);
                border-radius: 15px;
                padding: 2rem;
                margin-bottom: 2rem;
            }
            
            .action-buttons {
                display: flex;
                gap: 1rem;
                justify-content: center;
                flex-wrap: wrap;
                margin-bottom: 2rem;
            }
            
            .empty-state {
                text-align: center;
                padding: 3rem;
                color: var(--text-light);
            }
            
            .empty-state i {
                font-size: 4rem;
                color: var(--secondary-green);
                margin-bottom: 1rem;
            }
        </style>
        {% endblock %}
    </head>
    <body>
        <!-- Navigation -->
        <nav class="navbar navbar-expand-lg navbar-dark">
            <div class="container">
                <a class="navbar-brand" href="{{ path('app_dashboard') }}">
                    <i class="fas fa-leaf me-2"></i>
                    ÉcoCollecte
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_dashboard') }}">
                                <i class="fas fa-chart-line me-1"></i>
                                Tableau de Bord
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_point_collecte_index') }}">
                                <i class="fas fa-map-marker-alt me-1"></i>
                                Points de Collecte
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_depot_index') }}">
                                <i class="fas fa-book me-1"></i>
                                Journal des Dépôts
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>

        <main class="container-fluid py-4">
            {% block body %}{% endblock %}
        </main>

        <!-- Footer -->
        <footer class="mt-5" style="background: var(--dark-green); color: white;">
            <div class="container py-4">
                <div class="row">
                    <div class="col-md-6">
                        <h5><i class="fas fa-leaf me-2"></i>ÉcoCollecte</h5>
                        <p class="mb-0">Gestion durable des déchets pour un avenir plus vert.</p>
                    </div>
                    <div class="col-md-6 text-end">
                        <p class="mb-0">&copy; {{ "now"|date("Y") }} ÉcoCollecte. Tous droits réservés.</p>
                    </div>
                </div>
            </div>
        </footer>

        <!-- Bootstrap JS -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
        {% block javascripts %}{% endblock %}
    </body>
</html>