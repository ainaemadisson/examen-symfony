{% extends 'base.html.twig' %}

{% block title %}PointCollecte{% endblock %}

{% block body %}
    <h1>PointCollecte</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ point_collecte.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ point_collecte.type }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ point_collecte.adresse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_point_collecte_index') }}">back to list</a>

    <a href="{{ path('app_point_collecte_edit', {'id': point_collecte.id}) }}">edit</a>

    {{ include('point_collecte/_delete_form.html.twig') }}
{% endblock %}
